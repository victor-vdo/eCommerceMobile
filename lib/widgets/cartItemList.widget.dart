import 'package:flutter/material.dart';

import 'cartItem.widget.dart';

Widget cartItemList(context) {
  return ListView(
    children: [
      cartItem(),
      cartItem(),
      cartItem(),
      cartItem(),
      cartItem(),
      cartItem(),
      cartItem(),
    ],
  );
}
